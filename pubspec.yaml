name: ChatApp
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # StateManagement
  flutter_bloc:
  tuple:
  freezed_annotation:
  # JsonParser
  json_annotation:
  # Http Client
  dio:
  #Svg Loader
  flutter_svg:
  # Local Persist
  shared_preferences:
  cupertino_icons:
  firebase_core:
  firebase_auth:
  google_sign_in:
  cloud_firestore:
  fluttertoast:
  image_picker:
  firebase_storage:
  intl:
  firebase_messaging:
  flutter_local_notifications:
  photo_view:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.8.0"
  build_runner:
  json_serializable:
  freezed:

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icons/appchat.png"

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
    - assets/images/
